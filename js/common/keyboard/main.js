/**
 *
 *  The entrance of keyboard.
 *
 *  @author  Howard.Zuo
 *  @date    Dec 21th, 2014
 *
 **/
(function (define) {
    'use strict';

    define([
        './KeyBoard'
    ], function () {
        return [].slice.apply(arguments);
    });

}(define));