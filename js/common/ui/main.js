/**
 *
 *  The entrance of UI.
 *
 *  @author  Howard.Zuo
 *  @date    Dec 22th, 2014
 *
 **/
(function (define) {
    'use strict';

    define([
        './ProgressBar',
        './SectionIdentifier',
        './SlideAnimation'
    ], function () {
        return [].slice.apply(arguments);
    });

}(define));